{
  "name": "intercity-bus-booking-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Student microservices project for intercity bus booking with AI and MCP Server",
  "type": "module",
  "scripts": {
    "check:docs": "node scripts/check-docs.mjs",
    "compose:config": "docker compose config",
    "dev": "echo \"Run module services with npm run dev:web, dev:gateway, dev:analytics, and dev:mcp in separate terminals.\"",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:gateway": "npm --prefix services/graphql-gateway run dev",
    "dev:analytics": "npm --prefix services/analytics-service run dev",
    "dev:mcp": "npm --prefix services/mcp-server run dev"
  }
}
