# Infrastructure

This folder stores local infrastructure configuration and notes.

Current baseline:

```text
nginx/nginx.conf
rabbitmq/
kafka/
redis/
postgres/
```

Docker Compose is the source for local services:

```text
docker-compose.yml
```

Update `docs/README_SETUP.md` when ports, services, or environment variables change.
